// import express from "express";
// import { addExamName, deleteExamName, getAllExamNames, getExamNameById, updateExamName } from "../controllers/examNameController.js";

// const examNameRouter = express.Router();

// // CRUD Routes for Students
// examNameRouter.post("/", addExamName);
// examNameRouter.get("/", getAllExamNames);
// examNameRouter.get("/:id", getExamNameById);
// examNameRouter.put("/:id", updateExamName);
// examNameRouter.delete("/:id", deleteExamName);

// export default examNameRouter;
